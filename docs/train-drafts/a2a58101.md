# Train draft a2a58101

- **Package:** `secrets`
- **Kind:** human — Human concern — in-scope review comment; waiting for (or mid) grade.
- **Title:** `params` still ends up in the prepared url, and `raise_for_status()` includes th…
- **Human gold:** `params` still ends up in the prepared url, and `raise_for_status()` includes that full url in the exception. since the sync runner logs the exception, a 400/401 response will print the miradore api key. can we raise a sanitized error that keeps the status and request path but drops the query string, with a regression test that checks the key never appears?
- **PR:** https://github.com/cartography-cncf/cartography/pull/3117
- **Run:** `slice-1786520434695648000`

_Applied by `doomer train results apply`. Implement the generalized detection class; keep wording as evidence only._

## Miss brief — package `secrets`

### Human gold (example only — do not copy verbatim)

> `params` still ends up in the prepared url, and `raise_for_status()` includes that full url in the exception. since the sync runner logs the exception, a 400/401 response will print the miradore api key. can we raise a sanitized error that keeps the status and request path but drops the query string, with a regression test that checks the key never appears?

- PR: https://github.com/cartography-cncf/cartography/pull/3117
- PR title: feat(miradore): add Miradore MDM intel module
- Case: `cartography-pr-3117-r2`

### Spirit class: `technical-note`

This is **technical judgment** (design, API, clarity, approach) that is not pure LGTM and not a classic bug keyword hit. Capture the concern class and when a maintainer would interrupt the author.

### When to post this class of feedback

- The approach, API, layering, or clarity deserves maintainer pushback or guidance.
- The comment would change how the author finishes the work (not empty praise).
- Anchor to the relevant file when the human did.

### When **not** to post

- Empty process comments, pure social chat, or bot noise.
- Repository-specific lore that does not generalize.

### How to surface (implementation)

- Finding or observation with enough detail that an author knows what to change.
- Voice rewrite for surface form; keep the technical claim stable.

### Wording: keep spirit, vary surface form

The package must **not** hard-code the human sentence above in rules. For this policy/specialist package, keep the wording as training evidence only; do **not** append each concern to `agent/voice.md`.

Possible paraphrases for the same reasoning class (evidence only, not voice-bank entries):

1. "Why this approach and not the boring one?"
2. "I'm not convinced this belongs here."
3. "Explain the edge case or simplify."
4. "(spirit of) `params` still ends up in the prepared url, and `raise_for_status()` includes that full ur…"


### Acceptance for this train item

- [ ] On similar changes, the package emits this **class** of signal (not the exact quote)
- [ ] Posture matches **when to post** (no rubber-stamp on broken code; no silent ship on real bugs)
- [ ] Human wording remains evidence only; `agent/voice.md` is not expanded for this item
- [ ] Tests cover the **class**, not a single repository string match
